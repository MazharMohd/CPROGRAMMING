//to find the smallest of 2 no. using if else
#include<stdio.h>
int main()
{
int a,b;
printf("Enter any two no. =");
scanf("%d %d",&a,&b);
if(a<b)
{
    printf("smallest number is %d ",a);
}
else
{
    printf("smallest number is %d ",b);
}
}
